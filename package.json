{
  "name": "@io/core",
  "version": "1.0.0",
  "private": true,
  "workspaces": [
    "cli",
    "graph",
    "agent",
    "lib"
  ],
  "type": "module",
  "scripts": {
    "check": "bun run --cwd graph check",
    "agent": "io agent start"
  },
  "devDependencies": {
    "@io/cli": "workspace:"
  }
}
