{
  "name": "@io/core",
  "version": "1.0.0",
  "private": true,
  "workspaces": [
    "app",
    "cli",
    "graph",
    "agent",
    "tui",
    "config",
    "lib",
    "tsx"
  ],
  "type": "module",
  "scripts": {
    "check": "bun run --cwd graph check && bun run --cwd app check",
    "agent": "io agent start"
  },
  "devDependencies": {
    "@io/cli": "workspace:",
    "@io/lib": "workspace:*"
  }
}
